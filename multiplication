#include <stdio.h>

int main ()
{
	unsigned int N = 1;
	int value;

	printf( "N\t 10*N\t 100*N\t 1000*N\n" );

	while ( N <= 10 ) {
	
		printf( "%d\t %d\t %d\t %d\n", N, N*10, N*100, N*1000 );
	++N;
		}
	
}
